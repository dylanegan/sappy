sappy
===============

A ruby library providing a wrapper around the SiteUptime API.

USAGE
=====

require 'rubygems'
require 'sappy'

Sappy::Session.setup('email@email.com', 'password')

monitors = Sappy::Actions.monitor_list
